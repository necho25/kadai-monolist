<% if items.present? %> <!--present存在するという意味。なのであればtrue 対義語にblankがある-->
  <div class="row">
    <% items.each do |item| %>
      <div class="item">
        <div class="col-md-3 col-sm-4 col-xs-12">
          <div class="panel panel-default">
            <div class="panel-heading text-center">
              <img src="<%= item.image_url %>" alt="" >
            </div>
            <div class="panel-body">
              <% if item.persisted? %> <!--DBに保存されているか-->
                <p class="item-title"><%= link_to item.name, item %></p> <!--item.id itemのshowに飛ぶ-->
              <% else %>
                <p class="item-title"><%= item.name %></p>
              <% end %>
              <div class="buttons text-center">
                <% if logged_in? %>
                  <%= render 'ownerships/want_button', item: item %>
                  <%= render 'ownerships/have_button', item: item %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% end %>