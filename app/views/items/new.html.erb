<div class="search">
  <div class="row">
    <div class="text-center">
      <%= form_tag(new_item_path, method: :get, class: 'form-inline') do %><!--ID登録等と違い保存する必要がないのでﾓﾃﾞﾙを必要としないform_tagを使用-->
        <div class="form-group"><!--ここでroutesの設定と同様にどこのpathからどうするか設定する。設定しないとpostになる。今回はAPIから取得して表示の為のformなのでget-->
          <%= text_field_tag :keyword, @keyword, class: 'form-control input-lg', placeholder: '検索したいキーワードを入力', size: 40 %>
        </div>  <!--f.text_fieldではない事に注意!!上記ｵﾌﾟｼｮﾝのinput-lgはformの高さ-->
        <%= submit_tag '商品を検索', class: 'btn btn-success btn-lg' %><!--submitボタンもf.submitじゃない事に注意!!-->
        <% end %>
    </div>
  </div>
</div>

<%= render 'items', items: @items %>